# 3.3 — Sorties en console : formatage avancé (f-strings)

## Objectifs

- Comprendre ce qu'est une **f-string** et comment y insérer des variables.
- Contrôler la **largeur** et l'**alignement** d'un champ affiché.
- Formater des **nombres décimaux**, des **pourcentages** et des **grands nombres**.
- Construire un **tableau lisible** en console.

Tout au long de la section, nous utiliserons un même exemple : un petit **inventaire de fruits et légumes**, avec un nom de produit, un code de produit et un prix.

---

## 1. Qu'est-ce qu'une f-string ?

Une **f-string** (pour *formatted string*) est une chaîne de caractères précédée de la lettre `f`. À l'intérieur, tout ce qui est entre accolades `{ }` est **remplacé par sa valeur** au moment de l'affichage.

```python
print(f"Bienvenue au marché !")
```

Résultat :

```text
Bienvenue au marché !
```

Sans variable, une f-string se comporte comme une chaîne ordinaire. Son intérêt commence dès qu'on y insère des données.

---

## 2. Insérer une variable avec `{variable}`

```python
produit = "Pomme"

print(f"Produit : {produit}")
```

Résultat :

```text
Produit : Pomme
```

Python remplace `{produit}` par le **contenu** de la variable. Attention : sans le `f` devant les guillemets, Python afficherait littéralement `{produit}`.

```python
produit = "Pomme"

print("Produit : {produit}")   # ERREUR fréquente : le f est absent
```

Résultat :

```text
Produit : {produit}
```

---

## 3. Insérer plusieurs variables

On peut placer autant d'accolades que nécessaire dans la même f-string.

```python
produit = "Pomme"
code = "FRU-001"
prix = 1.25

print(f"Le produit {produit} (code {code}) coûte {prix} $.")
```

Résultat :

```text
Le produit Pomme (code FRU-001) coûte 1.25 $.
```

C'est déjà lisible… mais si on affiche plusieurs produits l'un sous l'autre, rien ne s'aligne. C'est exactement le problème que règlent les **spécificateurs de format**.

---

## 4. La largeur d'un champ

Après le nom de la variable, on ajoute un **deux-points** `:` suivi du format désiré.

```python
print(f"{variable:spécificateur}")
```

Le spécificateur le plus simple est un **nombre** : c'est la **largeur minimale** du champ, en caractères. Si la valeur est plus courte, Python complète avec des espaces ; si elle est plus longue, rien n'est coupé.

```python
produit = "Pomme"

print(f"|{produit:24}|")
```

Résultat :

```text
|Pomme                   |
```

Le mot `Pomme` occupe 5 caractères, et Python en ajoute 19 pour atteindre une largeur de 24.

> 💡 Les barres verticales `|` ne servent qu'à **voir** les limites du champ dans les exemples.

---

## 5. L'alignement

Trois symboles se placent juste avant la largeur pour choisir l'alignement.

### 5.1 Alignement à gauche : `:<24`

```python
produit = "Pomme"

print(f"|{produit:<24}|")
```

Résultat :

```text
|Pomme                   |
```

### 5.2 Alignement centré : `:^24`

```python
produit = "Pomme"

print(f"|{produit:^24}|")
```

Résultat :

```text
|         Pomme          |
```

### 5.3 Alignement à droite : `:>24`

```python
produit = "Pomme"

print(f"|{produit:>24}|")
```

Résultat :

```text
|                   Pomme|
```

> 📌 **Bonne habitude** : toujours écrire `<`, `^` ou `>` explicitement. Sans cela, Python aligne le texte à gauche mais les nombres à droite, et les colonnes deviennent incohérentes.

---

## 6. Le caractère de remplissage (*padding*)

Par défaut, Python remplit l'espace libre avec des **espaces**. On peut choisir un autre caractère en le plaçant **avant** le symbole d'alignement.

```python
produit = "Pomme"

print(f"|{produit:-^24}|")
print(f"|{produit:.<24}|")
print(f"|{produit:*>24}|")
```

Résultat :

```text
|---------Pomme----------|
|Pomme...................|
|*******************Pomme|
```

La lecture se fait dans l'ordre : `-` est le caractère de remplissage, `^` l'alignement, `24` la largeur.

---

## 7. Les nombres décimaux : `:.2f`

Un prix s'écrit avec **deux décimales**, toujours, même quand elles valent zéro. Le spécificateur `.2f` s'en charge :

- `.2` : deux chiffres après le point ;
- `f` : afficher la valeur en notation décimale *(float)*.

```python
prix = 2.5

print(f"Prix brut    : {prix}")
print(f"Prix formaté : {prix:.2f} $")
```

Résultat :

```text
Prix brut    : 2.5
Prix formaté : 2.50 $
```

Le formatage **arrondit** au besoin :

```python
prix = 1.9999

print(f"{prix:.2f}")
```

Résultat :

```text
2.00
```

---

## 8. Combiner largeur, alignement et décimales : `:>10.2f`

Les spécificateurs se combinent, dans l'ordre : **alignement**, puis **largeur**, puis **décimales**.

```python
prix = 3.5

print(f"|{prix:>10.2f}|")
```

Résultat :

```text
|      3.50|
```

Décortiquons `{prix:>10.2f}` :

| Morceau | Signification |
| --- | --- |
| `prix` | la variable à afficher |
| `:` | début du spécificateur de format |
| `>` | aligner à **droite** |
| `10` | réserver une largeur de **10 caractères** |
| `.2f` | afficher le nombre avec **2 décimales** |

C'est la formule à retenir pour une colonne de prix : les points décimaux s'alignent parfaitement les uns sous les autres.

---

## 9. Le séparateur de milliers : `:,`

```python
ventes = 1234567.891

print(f"{ventes:,}")
print(f"{ventes:,.2f}")
```

Résultat :

```text
1,234,567.891
1,234,567.89
```

> ℹ️ Python utilise la virgule comme séparateur de milliers et le point comme séparateur décimal, à l'anglaise.

---

## 10. Le pourcentage : `:.1%`

Le spécificateur `%` multiplie la valeur par 100 et ajoute le symbole `%`. Le chiffre avant le `%` indique le nombre de décimales.

```python
rabais = 0.155

print(f"Rabais : {rabais:.1%}")
print(f"Rabais : {rabais:.0%}")
```

Résultat :

```text
Rabais : 15.5%
Rabais : 16%
```

Attention : la valeur de départ doit être une **proportion** (`0.155`), pas un nombre déjà exprimé en pourcentage (`15.5`).

---

## 11. Le remplissage par des zéros : `:0>6`

Très utile pour des codes ou des numéros de longueur fixe.

```python
numero = 42

print(f"{numero:0>6}")
```

Résultat :

```text
000042
```

Ici, `0` est le caractère de remplissage, `>` l'alignement à droite et `6` la largeur totale.

---

## 12. Une expression ou un calcul dans une f-string

Les accolades n'acceptent pas seulement des variables : elles acceptent **n'importe quelle expression Python**.

```python
prix = 1.25
quantite = 4

print(f"Total : {prix * quantite:.2f} $")
print(f"Prix avec taxes : {prix * 1.14975:.2f} $")
print(f"Nom en majuscules : {'pomme'.upper()}")
```

Résultat :

```text
Total : 5.00 $
Prix avec taxes : 1.44 $
Nom en majuscules : POMME
```

> 💡 Garde les expressions **courtes** dans les f-strings. Un calcul long gagne à être fait avant, dans une variable bien nommée.

---

## 13. Déboguer avec `{variable=}`

En ajoutant un `=` après le nom de la variable, Python affiche **le nom ET la valeur**. C'est un outil de débogage très pratique.

```python
produit = "Carotte"
prix = 1.10

print(f"{produit=}")
print(f"{prix=}")
print(f"{prix * 3=:.2f}")
```

Résultat :

```text
produit='Carotte'
prix=1.1
prix * 3=3.30
```

Cette syntaxe sert à **vérifier** ce que contient une variable pendant la mise au point d'un programme, et non à produire l'affichage final destiné à l'utilisateur.

---

## 14. Tableau synthèse

| Syntaxe | Description | Exemple Python | Résultat |
| --- | --- | --- | --- |
| `{variable}` | Insère la valeur de la variable | `print(f"{produit}")` | `Pomme` |
| `{v1} {v2}` | Insère plusieurs variables | `print(f"{produit} {code}")` | `Pomme FRU-001` |
| `:24` | Largeur minimale de 24 caractères | `print(f"[{produit:24}]")` | `[Pomme                   ]` |
| `:<24` | Aligné à gauche sur 24 caractères | `print(f"[{produit:<24}]")` | `[Pomme                   ]` |
| `:^24` | Centré sur 24 caractères | `print(f"[{produit:^24}]")` | `[         Pomme          ]` |
| `:>24` | Aligné à droite sur 24 caractères | `print(f"[{produit:>24}]")` | `[                   Pomme]` |
| `:-^24` | Centré, rempli avec des tirets | `print(f"[{produit:-^24}]")` | `[---------Pomme----------]` |
| `:.2f` | Nombre décimal à 2 décimales | `print(f"{prix:.2f}")` | `1.25` |
| `:>10.2f` | Aligné à droite, largeur 10, 2 décimales | `print(f"[{prix:>10.2f}]")` | `[      1.25]` |
| `:,` | Séparateur de milliers | `print(f"{1234567:,}")` | `1,234,567` |
| `:,.2f` | Milliers et 2 décimales | `print(f"{1234.5:,.2f}")` | `1,234.50` |
| `:.1%` | Pourcentage à 1 décimale | `print(f"{0.155:.1%}")` | `15.5%` |
| `:0>6` | Rempli de zéros sur 6 caractères | `print(f"{42:0>6}")` | `000042` |
| `{calcul}` | Expression évaluée directement | `print(f"{2 * 3}")` | `6` |
| `{variable=}` | Nom et valeur (débogage) | `print(f"{prix=}")` | `prix=1.25` |

*Dans ce tableau, les crochets `[ ]` remplacent les barres verticales `|`, qui servent déjà à séparer les colonnes du tableau Markdown. Dans ton code, tu peux utiliser l'un ou l'autre.*

---

## 15. Exemple complet : un tableau d'inventaire

Assemblons tout ce qui précède pour produire un vrai tableau, avec des **séparateurs verticaux** `|` et des **lignes horizontales** obtenues par `print("-" * 76)`.

```python
print("-" * 76)
print(f"|{'Produit':<24}|{'Code':^24}|{'Prix':>24}|")
print("-" * 76)
print(f"|{'Pomme':<24}|{'FRU-001':^24}|{1.25:>24.2f}|")
print(f"|{'Banane':<24}|{'FRU-002':^24}|{0.75:>24.2f}|")
print(f"|{'Orange':<24}|{'FRU-003':^24}|{2.50:>24.2f}|")
print(f"|{'Carotte':<24}|{'LEG-001':^24}|{1.10:>24.2f}|")
print(f"|{'Brocoli':<24}|{'LEG-002':^24}|{3.45:>24.2f}|")
print(f"|{'Tomate':<24}|{'LEG-003':^24}|{2.99:>24.2f}|")
print("-" * 76)
```

Résultat :

```text
----------------------------------------------------------------------------
|Produit                 |          Code          |                    Prix|
----------------------------------------------------------------------------
|Pomme                   |        FRU-001         |                    1.25|
|Banane                  |        FRU-002         |                    0.75|
|Orange                  |        FRU-003         |                    2.50|
|Carotte                 |        LEG-001         |                    1.10|
|Brocoli                 |        LEG-002         |                    3.45|
|Tomate                  |        LEG-003         |                    2.99|
----------------------------------------------------------------------------
```

### Pourquoi 76 ?

Chaque ligne du tableau est composée de :

- 3 colonnes de **24** caractères chacune → 72 ;
- 4 barres verticales `|` → 4.

Total : **76** caractères. C'est pourquoi `"-" * 76` produit une ligne exactement de la bonne longueur.

### Le rôle de chaque colonne

| Colonne | Format | Pourquoi |
| --- | --- | --- |
| Produit | `:<24` | Le texte se lit mieux aligné à **gauche** |
| Code | `:^24` | Un code court est agréable **centré** |
| Prix | `:>24.2f` | Les nombres s'alignent à **droite**, décimales alignées |

### Avec des variables

Le même tableau, en stockant les valeurs dans des variables (comme dans un vrai programme) :

```python
produit = "Pomme"
code = "FRU-001"
prix = 1.25

print(f"|{produit:<24}|{code:^24}|{prix:>24.2f}|")
```

Résultat :

```text
|Pomme                   |        FRU-001         |                    1.25|
```

> 🔎 Le prix doit être un **`float`** (`1.25`, et non `"1.25"`). Le spécificateur `.2f` ne fonctionne pas sur une chaîne de caractères : Python signalerait une erreur.

---

## 16. À retenir

> ### 📌 À retenir — les f-strings
>
> - Une f-string commence par **`f`** avant les guillemets : `f"..."`. Sans ce `f`, rien n'est remplacé.
> - Une variable s'insère avec **`{variable}`** ; on peut en mettre autant qu'on veut.
> - Le format s'écrit après un **deux-points** : `{variable:format}`.
> - L'ordre du format est : **remplissage**, **alignement**, **largeur**, **décimales** → `{prix:*>10.2f}`.
> - Alignements : **`<`** à gauche, **`^`** au centre, **`>`** à droite. Précise-le **toujours**.
> - `.2f` = deux décimales (idéal pour des prix), `,` = séparateur de milliers, `.1%` = pourcentage.
> - `{variable=}` affiche le nom et la valeur : pratique pour **déboguer**.
> - Pour un tableau : la même largeur pour toutes les cellules d'une colonne, et une ligne de séparation avec `print("-" * n)`.

---

## Exercices d'application

### Exercice 1 — 🟢 Facile

Crée une variable `produit` contenant `"Brocoli"` et affiche la phrase suivante à l'aide d'une f-string.

Résultat attendu :

```text
Produit : Brocoli
```

### Exercice 2 — 🟢 Facile

Affiche le mot `Tomate` centré dans un champ de 20 caractères, encadré par des barres `|`.

Résultat attendu :

```text
|       Tomate       |
```

### Exercice 3 — 🟢 Facile

Affiche le code `LEG-002` aligné à droite dans un champ de 12 caractères rempli de points, encadré par des barres `|`.

Résultat attendu :

```text
|.....LEG-002|
```

### Exercice 4 — 🟡 Moyen

La variable `prix` vaut `2.5`. Affiche-la alignée à droite sur 10 caractères avec exactement 2 décimales, encadrée par des barres `|`.

Résultat attendu :

```text
|      2.50|
```

### Exercice 5 — 🟡 Moyen

Le numéro de commande est `87`. Affiche-le sur 6 caractères, complété par des zéros devant.

Résultat attendu :

```text
000087
```

### Exercice 6 — 🟡 Moyen

Les variables `prix` et `quantite` valent respectivement `1.10` et `7`. Affiche le total (prix × quantité) avec 2 décimales, en effectuant le calcul **directement dans la f-string**.

Résultat attendu :

```text
Total : 7.70 $
```

### Exercice 7 — 🔴 Avancé

Le chiffre d'affaires du marché est de `1548732.4` dollars et la marge de profit est de `0.083`. Affiche les deux lignes suivantes.

Résultat attendu :

```text
Chiffre d'affaires : 1,548,732.40 $
Marge de profit    : 8.3%
```

### Exercice 8 — 🔴 Avancé

Reproduis les deux premières lignes du tableau d'inventaire (la ligne de tirets et la ligne d'en-têtes), avec trois colonnes de 24 caractères : `Produit` à gauche, `Code` centré et `Prix` à droite.

Résultat attendu :

```text
----------------------------------------------------------------------------
|Produit                 |          Code          |                    Prix|
```

---

## 🎥 Vidéo explicative : Les spécificateurs de format

[![Regarder](https://img.youtube.com/vi/jH6HlABhvxE/maxresdefault.jpg)](https://youtu.be/jH6HlABhvxE)

*Cette vidéo explique le formatage des chaînes en Python, dont les f-strings et leurs spécificateurs de format.*

---

## Réponses aux exercices

### Exercice 1

```python
produit = "Brocoli"
print(f"Produit : {produit}")
```

### Exercice 2

```python
print(f"|{'Tomate':^20}|")
```

### Exercice 3

```python
print(f"|{'LEG-002':.>12}|")
```

### Exercice 4

```python
prix = 2.5
print(f"|{prix:>10.2f}|")
```

### Exercice 5

```python
numero = 87
print(f"{numero:0>6}")
```

### Exercice 6

```python
prix = 1.10
quantite = 7
print(f"Total : {prix * quantite:.2f} $")
```

### Exercice 7

```python
chiffre_affaires = 1548732.4
marge = 0.083
print(f"Chiffre d'affaires : {chiffre_affaires:,.2f} $")
print(f"Marge de profit    : {marge:.1%}")
```

### Exercice 8

```python
print("-" * 76)
print(f"|{'Produit':<24}|{'Code':^24}|{'Prix':>24}|")
```
